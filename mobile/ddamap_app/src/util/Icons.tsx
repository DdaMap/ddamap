import FontAwesome6 from '@react-native-vector-icons/fontawesome6';

const Icons = {
  map: (color: string = 'black', size: number = 30) => (
    <FontAwesome6 name="map" color={color} size={size} />
  ),
};

export default Icons;
